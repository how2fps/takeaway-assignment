export type Redemption = {
       staff_pass_id: string;
       team_name: string;
       redeemed_at: number;
};

export type Staff = {
       staff_pass_id: string;
       team_name: string;
       created_at: number;
};
